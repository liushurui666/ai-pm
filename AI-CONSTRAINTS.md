# AI 约束（全项目）

## 适用范围

本约束适用于本项目全部代码与配置文件的新增与修改，不再局限于单个文件。

## 1. 结构与组件化

- 禁止新增文件承载过重业务逻辑（页面 + 表单 + 表格 + 抽屉 + 工具函数混写）。
- 复杂页面必须按职责拆分为主容器、子组件、hook、utils、types、constants 等层级。
- 如果一个文件出现多个职责混合，优先拆分成更小模块。
- 文件建议阈值：
  - 超过 300 行且逻辑明显多职责时应拆分。
  - 超过 500 行应优先重构拆分。
- 所有 React 组件必须使用组件目录格式：`组件目录/index.tsx` + `组件目录/index.less`。
- 新增组件禁止直接创建 `SomeComponent.tsx`、`some-component.tsx`、`SomeComponent.css` 等平铺组件文件。
- 修改既有组件时，如果涉及组件拆分、新增子组件或样式重构，必须同步迁移到 `index.tsx` + `index.less` 目录结构。
- `index.tsx` 负责组件入口和必要组合逻辑，样式必须放在同目录 `index.less`，避免继续扩大全局样式文件。
- `project-management-platform` 及其子模块执行上述规则，其他模块同样执行。
- `src/lib` 禁止继续新增根目录平铺工具文件。工具代码必须按领域放入子目录，例如：
  - AI 与模型调用：`src/lib/ai/`
  - 认证与会话：`src/lib/auth/`
  - 数据库与 Prisma：`src/lib/database/`
  - 文档解析：`src/lib/documents/`
  - 飞书集成：`src/lib/feishu/`
  - 权限与访问控制：`src/lib/access/`
  - 主题偏好：`src/lib/theme/`
  - 周报与报表：`src/lib/reports/`
  - Bug 自动修复：`src/lib/bug-fix-jobs/`
  - Git 平台：`src/lib/git-providers/`
  - 需求分析：`src/lib/requirements/`
- 新增 lib 工具时必须优先复用现有领域目录；确实出现新领域时，先创建清晰命名的领域目录，再放入具体工具文件。
- 修改既有 lib 工具时，如果发现它仍在错误领域或根目录平铺位置，必须同步迁移到合适领域目录，并更新全项目 import。

## 2. 中文注释

- 项目所有新增和修改的代码都必须标明中文注释，注释要详细说明业务意图、边界条件和关键取舍。
- 禁止新增只重复代码字面意思的空泛注释，例如“设置变量”“调用函数”；注释必须解释“为什么这样处理”和“不这样处理会有什么问题”。
- 以下内容必须有详细中文注释说明：
  - 模块入口的职责边界
  - 组件定义
  - hook 与工具函数
  - 复杂条件分支
  - 接口对接/数据转换/边界处理逻辑
  - 非直观的状态变更流程
  - 数据库读写、事务、迁移和种子数据逻辑
  - 权限判断、登录会话、第三方集成和安全限制
  - AI 调用、Prompt 拼装、兜底逻辑和自动修复 Runner 约束
  - 时间、版本、负责人、项目范围等容易产生歧义的计算规则
- 修改旧代码时，如果触碰到缺少中文注释的函数、组件、复杂分支或数据转换逻辑，必须顺手补齐详细中文注释。
- 允许不注释纯类型声明、简单常量枚举、样式类名和显而易见的一行 UI 文案；但只要存在业务判断、跨模块数据流或副作用，就必须补中文注释。
- 注释语言统一使用中文；除第三方协议名、API 字段名、命令、错误码等不可翻译内容外，不新增英文注释。

## 3. 质量与提交

- 任何改动必须先在本地通过项目标准验证后再提交。
- 至少执行：
  - lint
  - 若改动涉及构建链路，则同时执行 build 或相关测试
- 校验未通过不得提交与推送。

## 4. 提交与推送

- 每次完成代码、文档或配置修改后，必须自动执行 git commit 和 git push，不需要等待额外提醒。
- commit 信息必须使用中文，且要准确说明本次改动内容。
- 推送前需确认工作区只保留本次任务相关修改。
- 本地验证通过后，必须 push 代码；校验失败时不得提交与推送。

## 5. 兼容性与可维护性

- 优先复用既有公共组件与工具，减少重复实现。
- 避免循环依赖，保持导入方向清晰可追踪。
- 命名与文件布局尽量与现有项目约定一致。
